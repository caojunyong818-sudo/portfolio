# 作品集上线说明

这个目录是整理后的 GitHub Pages 发布包。

## 推荐上线方式

1. 在 GitHub 新建一个公开仓库，例如 `portfolio`.
2. 将本目录 `portfolio-publish` 内的所有文件上传到仓库根目录。
3. 进入仓库 `Settings -> Pages`。
4. `Build and deployment` 选择 `Deploy from a branch`。
5. 分支选择 `main`，目录选择 `/root`，保存。
6. 等待 GitHub Pages 部署完成，访问 `https://你的用户名.github.io/portfolio/`。

## 本轮更新

- 联系邮箱统一为 `2361423832@qq.com`，全站采用黑白、浅色网格与荧光绿视觉系统。
- 首页加入无需外部依赖的 Canvas 粒子雕塑，支持指针互动、暂停和减少动态效果设置；自动停止不可见动画。
- 保留视觉风格，增加短遮罩转场、分层入场、作品筛选重排及图像浏览动效；支持减少动态效果和浏览器历史返回。
- 首页突出《时间边界》，作品目录共 10 个项目；新增 MetaHuman 插件与类魂关卡项目。
- 每个项目有独立页面；已有图集支持图片放大、前后切换与键盘操作。
- 已同步共享样式、脚本、51 张网页图片与中英文项目介绍，包含研究、设计思路、实现过程、图片说明和章节导航。
- 补充《调和者》的双世界玩法、关卡方案与技术实现，以及蒙古族 VR 展馆的调研、资产制作、参观路线和交互流程；类魂关卡项目已加入用户提供的完整封面。
- 导航右侧可切换中文／英文；语言偏好、项目位置与筛选条件会保留。英文静态页面位于 `个人简历/en/`。
- 更新源码目录的内容后，运行 `tools/build_portfolio.py` 可重新同步发布页面。

## 注意

- 已删除首页个人简介里的视频框架。
- 发布包没有包含超过 100MB 的源视频文件，避免 GitHub 上传失败。
- 阿里云通义千问网页展示入口已改为线上链接。
